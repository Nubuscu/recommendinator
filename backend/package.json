{
  "name": "recommendinator",
  "version": "0.0.1",
  "description": "recommendation server/service to generate spotify playlists from an existing playlist",
  "main": "server.js",
  "scripts": {
    "build": "babel ../backend -d dist --ignore node_modules,dist",
    "serve": "node dist/server.js",
    "start": "npm run build && npm run serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nubuscu",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5"
  }
}
